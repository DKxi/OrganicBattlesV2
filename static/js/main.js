const $=s=>document.querySelector(s);let session=null,game=null;
const api=async(path,body={})=>{const payload={...body};const sid=payload.session_id;delete payload.session_id;const url=sid?`${path}?session_id=${encodeURIComponent(sid)}`:path;const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw Error((await r.json()).detail||'Action unavailable');return r.json()};
const spells=[['fire-spark','Fire Spark','BASIC','20 DMG'],['acid-shot','Acid Shot','BASIC','20 DMG'],['carbon-punch','Carbon Punch','BASIC','20 DMG'],['resonance-burst','Resonance Burst','MED','30 DMG'],['nucleophile-strike','Nucleophile Strike','MED','30 DMG'],['chiral-slash','Chiral Slash','MED','30 DMG'],['mechanism-storm','Mechanism Storm','STRONG','45 DMG'],['stereochemical-rift','Stereochemical Rift','STRONG','45 DMG'],['spectral-obliteration','Spectral Obliteration','STRONG','45 DMG']];
const avatarPalette={body:{arc:'#29556b',scholar:'#66528a'},skin:{warm:'#ffd4b2',deep:'#8e563f',gold:'#d8a25e'},hair:{nebula:'#6c3d79',copper:'#b7653f',silver:'#b8c8d2'},outfit:{coat:'#29556b',hoodie:'#493b78',shirt:'#245e62'},accessory:{goggles:'#36e5d0',gloves:'#ff9f5a',backpack:'#a67cff'}};
function updateAvatarPreview(){const figure=$('.avatar-preview .avatar-orb');if(!figure)return;const values=Object.fromEntries(['body','skin','hair','outfit','accessory'].map(id=>[id,$('#'+id).value]));figure.className='avatar-figure large';figure.dataset.body=values.body;figure.dataset.outfit=values.outfit;figure.innerHTML='<div class="avatar-aura"></div><div class="avatar-body"></div><div class="avatar-head"><div class="avatar-hair"></div></div><div class="avatar-accessory"></div>';Object.entries(values).forEach(([part,value])=>figure.style.setProperty(`--avatar-${part}`,avatarPalette[part][value]));let label=$('#avatar-preview-label');if(!label){label=document.createElement('span');label.id='avatar-preview-label';figure.parentElement.append(label)}label.textContent=`${values.body.toUpperCase()} // ${values.outfit.toUpperCase()} // ${values.accessory.toUpperCase()}`}
function ensureExplanationUi(){const brand=$('.brand');if(!brand)return;let button=$('#view-explanation');if(!button){button=document.createElement('button');button.id='view-explanation';button.className='header-help';button.textContent='VIEW EXPLANATION';brand.parentElement.insertBefore(button,brand.nextSibling)}let modal=$('#explanation-modal');if(!modal){modal=document.createElement('div');modal.id='explanation-modal';modal.className='hidden';modal.innerHTML='<div class="modal-card"><button id="close-explanation" class="modal-close" aria-label="Close explanation">×</button><div class="eyebrow">ORGO // CONCEPT REVIEW</div><h2 id="explanation-title">WHY THIS ANSWER?</h2><p id="explanation-question" class="modal-question"></p><div class="modal-answer"><span class="hint">CORRECT ANSWER</span><strong id="explanation-answer"></strong></div><p id="explanation-copy"></p><button id="modal-done" class="primary">BACK TO BATTLE</button></div>';$('#app').append(modal);$('#close-explanation').onclick=closeExplanation;$('#modal-done').onclick=closeExplanation;modal.onclick=e=>{if(e.target===modal)closeExplanation()}}button.onclick=()=>{if(window.lastExplanation)showExplanation(window.lastExplanation)}}
function closeExplanation(){$('#explanation-modal')?.classList.add('hidden')}
function showExplanation(result){window.lastExplanation=result;ensureExplanationUi();$('#explanation-question').textContent=result.question_prompt||'Review the chemistry concept from the last trial.';$('#explanation-answer').textContent=result.correct_answer;$('#explanation-copy').textContent=result.explanation;$('#explanation-modal').classList.remove('hidden')}
ensureExplanationUi();
['body','skin','hair','outfit','accessory'].forEach(id=>$('#'+id).addEventListener('change',updateAvatarPreview));
$('#start').onclick=async()=>{session=await api('/api/game/new',{});$('#boot').classList.add('hidden');$('#avatar-creator').classList.remove('hidden');updateAvatarPreview()};
$('#accept-avatar').onclick=async()=>{const avatar=Object.fromEntries(['body','skin','hair','outfit','accessory'].map(id=>[id,$('#'+id).value]));avatar.aura='teal';session=await api('/api/avatar/finalize',{session_id:session.session_id,...avatar});$('#avatar-creator').classList.add('hidden');$('#game-shell').classList.remove('hidden');startPhaser();render(session)};
function startPhaser(){game=new Phaser.Game({type:Phaser.AUTO,parent:'phaser',width:900,height:520,backgroundColor:'#0b1e2c',scale:{mode:Phaser.Scale.RESIZE,autoCenter:Phaser.Scale.CENTER_BOTH},scene:{preload(){this.load.image('arena','/static/assets/battle-arena.png')},create(){this.arena=this.add.image(this.scale.width/2,this.scale.height/2,'arena').setOrigin(.5);this.resizeArena();this.scale.on('resize',()=>this.resizeArena())},resizeArena(){if(!this.arena)return;this.arena.setPosition(this.scale.width/2,this.scale.height/2);this.arena.setDisplaySize(this.scale.width,this.scale.height)}}})}
function render(s){session=s;$('#chapter-label').textContent=`CHAPTER ${s.chapter} / ${s.chapter_name}`;$('#avatar-panel').innerHTML=`<div class="avatar-card"><div class="avatar-orb"></div><div><div class="avatar-name">FIELD ALCHEMIST</div><div class="avatar-sub">${s.player.hp} / ${s.player.max_hp} HP</div></div></div>`;$('#log').innerHTML=s.log.map(x=>`<div class="log-line">${x}</div>`).join('');renderSpells(s);renderQuestion(s);drawScene(s)}
function renderSpells(s){$('#spells').innerHTML=`<div class="control-panel"><div class="control-title">ARSENAL // SELECT A SPELL</div><div class="spell-grid">${spells.map(([id,n,t,d])=>{let cd=s.cooldowns?.[id]||0;return `<button class="spell" data-spell="${id}" ${cd?'disabled':''}><div class="spell-name">${n}</div><div class="spell-meta">${t} · ${cd?cd+'s':d}</div></button>`}).join('')}</div></div>`;document.querySelectorAll('[data-spell]').forEach(b=>b.onclick=async()=>{try{render(await api('/api/battle/select-spell',{session_id:session.session_id,spell_id:b.dataset.spell}))}catch(e){alert(e.message)}})}
function renderQuestion(s){const q=s.question;$('#question').innerHTML=`<div class="control-panel">${q?`<div class="control-title">VOCABULARY TRIAL // ONE ATTEMPT</div><div class="question">${q.prompt}</div><div class="answers">${q.choices.map((a,i)=>`<button class="answer" data-answer="${a}"><span class="hint">${'ABCD'[i]}</span><br>${a}</button>`).join('')}</div>`:`<div class="control-title">BATTLE STATUS</div><div class="question">${s.boss.name} awaits your next spell.</div><div class="hint">Choose a spell above to reveal a chemistry trial.</div>`}</div>`;document.querySelectorAll('[data-answer]').forEach(b=>b.onclick=async()=>{try{const r=await api('/api/battle/answer',{session_id:session.session_id,answer:b.dataset.answer});render(r);showOutcome(r)}catch(e){alert(e.message)}})}
function showOutcome(r){const msg=r.defeated?`VICTORY — ${r.boss.name} defeated.`:r.defeat?'DEFEAT — retry to regroup.':r.correct?`DIRECT HIT — ${r.damage} damage. ${r.boss_hit?'Counterattack!':'Boss missed!'}`:`SPELL FIZZLE — correct answer: ${r.correct_answer}`;setTimeout(()=>{if(r.defeat){if(confirm(msg+' Retry?'))api('/api/battle/retry',{session_id:session.session_id}).then(render)}else if(r.defeated){if(confirm(msg+' Continue to next node?'))api('/api/battle/next-turn',{session_id:session.session_id}).then(x=>{if(x.victory)alert('SPECTRAL CHAMPION — all chapters complete!');render(x)})}else alert(msg)},200)}
function drawScene(s){if(!game?.scene?.scenes?.[0])return;const sc=game.scene.scenes[0],w=sc.scale.width,h=sc.scale.height;sc.children.list.filter(x=>x.getData?.('dynamic')).forEach(x=>x.destroy());const col=Phaser.Display.Color.HexStringToColor(s.chapter_color).color;const boss=sc.add.container(w*.72,h*.42).setData('dynamic',true);const glow=sc.add.circle(0,0,Math.min(105,w*.16),col,.12).setStrokeStyle(3,col,.8);boss.add([glow,sc.add.ellipse(0,20,150,190,0x182a42).setStrokeStyle(4,col),sc.add.circle(0,-18,28,0x07111c).setStrokeStyle(4,0xff9f5a),sc.add.text(0,130,s.boss.name,{fontFamily:'Space Grotesk',fontSize:'20px',color:'#eaf7f5'}).setOrigin(.5),sc.add.text(0,158,`BOSS HP ${s.boss.hp} / ${s.boss.max_hp}`,{fontFamily:'DM Mono',fontSize:'12px',color:'#ff9f5a'}).setOrigin(.5)]);sc.tweens.add({targets:glow,scale:1.15,alpha:.3,duration:1200,yoyo:true,repeat:-1});const avatar=s.avatar||{};const avatarColor=(part,fallback)=>Phaser.Display.Color.HexStringToColor(avatarPalette[part]?.[avatar[part]]||fallback).color;const player=sc.add.container(w*.23,h*.62).setData('dynamic',true);const aura=sc.add.circle(0,0,64,avatarColor('accessory','#36e5d0'),.12);const body=sc.add.ellipse(0,25,80,120,avatarColor('outfit','#29556b'));const head=sc.add.circle(0,-45,34,avatarColor('skin','#ffd4b2'));const hair=sc.add.arc(0,-55,30,200,340,false,avatarColor('hair','#6c3d79'));const accessory=sc.add.rectangle(0,-45,52,6,avatarColor('accessory','#36e5d0'),.85);player.add([aura,body,head,hair,accessory,sc.add.text(0,110,`YOU ${s.player.hp} HP`,{fontFamily:'DM Mono',fontSize:'12px',color:'#36e5d0'}).setOrigin(.5)])}
function showOutcome(r){const msg=r.defeated?`VICTORY — ${r.boss.name} defeated.`:r.defeat?'DEFEAT — retry to regroup.':r.correct?`DIRECT HIT — ${r.damage} damage. ${r.boss_hit?'Counterattack!':'Boss missed!'}`:`SPELL FIZZLE — correct answer: ${r.correct_answer}`;if(!r.correct&&!r.defeat&&!r.defeated){window.lastExplanation=r;const button=$('#view-explanation');if(button){button.textContent='EXPLANATION';button.classList.add('available')}return alert(msg+' Click EXPLANATION beside the title to review the concept.')}setTimeout(()=>{if(r.defeat){if(confirm(msg+' Retry?'))api('/api/battle/retry',{session_id:session.session_id}).then(render)}else if(r.defeated){if(confirm(msg+' Continue to next node?'))api('/api/battle/next-turn',{session_id:session.session_id}).then(x=>{if(x.victory)alert('SPECTRAL CHAMPION — all chapters complete!');render(x)})}else alert(msg)},200)}
function showOutcome(r){const msg=r.defeated?`VICTORY — ${r.boss.name} defeated.`:r.defeat?'DEFEAT — retry to regroup.':r.correct?`DIRECT HIT — ${r.damage} damage. ${r.boss_hit?'Counterattack!':'Boss missed!'}`:`SPELL FIZZLE — correct answer: ${r.correct_answer}`;if(!r.correct&&!r.defeat&&!r.defeated){window.lastExplanation=r;ensureExplanationUi();const headerButton=$('#view-explanation');if(headerButton){headerButton.textContent='EXPLANATION';headerButton.classList.add('available')}const inline=document.createElement('button');inline.type='button';inline.className='explanation-inline';inline.textContent='EXPLANATION';inline.onclick=()=>showExplanation(r);$('#question .control-panel')?.append(inline);return alert(msg+' Use the EXPLANATION button to review the concept.')}setTimeout(()=>{if(r.defeat){if(confirm(msg+' Retry?'))api('/api/battle/retry',{session_id:session.session_id}).then(render)}else if(r.defeated){if(confirm(msg+' Continue to next node?'))api('/api/battle/next-turn',{session_id:session.session_id}).then(x=>{if(x.victory)alert('SPECTRAL CHAMPION — all chapters complete!');render(x)})}else alert(msg)},200)}
function showOutcome(r){const msg=r.defeated?`VICTORY — ${r.boss.name} defeated.`:r.defeat?'DEFEAT — retry to regroup.':r.correct?`DIRECT HIT — ${r.damage} damage. ${r.boss_hit?'Counterattack!':'Boss missed!'}`:`SPELL FIZZLE — correct answer: ${r.correct_answer}`;if(!r.correct&&!r.defeat&&!r.defeated){window.lastExplanation=r;ensureExplanationUi();const headerButton=$('#view-explanation');if(headerButton){headerButton.textContent='EXPLANATION';headerButton.classList.add('available')}let action=$('#explanation-action');if(!action){action=document.createElement('button');action.id='explanation-action';action.className='explanation-action';action.textContent='EXPLANATION';action.onclick=()=>showExplanation(window.lastExplanation);document.body.append(action)}action.hidden=false;return alert(msg+' Click the EXPLANATION button to review the concept.')}setTimeout(()=>{if(r.defeat){if(confirm(msg+' Retry?'))api('/api/battle/retry',{session_id:session.session_id}).then(render)}else if(r.defeated){if(confirm(msg+' Continue to next node?'))api('/api/battle/next-turn',{session_id:session.session_id}).then(x=>{if(x.victory)alert('SPECTRAL CHAMPION — all chapters complete!');render(x)})}else alert(msg)},200)}
function renderIncorrectExplanation(r){const panel=$('#question .control-panel');if(!panel)return;panel.innerHTML=`<div class="control-title">BATTLE STATUS // CONCEPT REVIEW</div><div class="question explanation-heading">SPELL FIZZLE — REVIEW THE TRIAL</div><div class="explanation-status"><div><span class="hint">CORRECT ANSWER</span><strong>${r.correct_answer}</strong></div><p>${r.explanation}</p><span class="hint">NEXT TIME: Identify the key chemistry clue in the definition before choosing.</span></div>`}
function showOutcome(r){const msg=r.defeated?`VICTORY — ${r.boss.name} defeated.`:r.defeat?'DEFEAT — retry to regroup.':r.correct?`DIRECT HIT — ${r.damage} damage. ${r.boss_hit?'Counterattack!':'Boss missed!'}`:`SPELL FIZZLE — correct answer: ${r.correct_answer}`;if(!r.correct&&!r.defeated){renderIncorrectExplanation(r);const headerButton=$('#view-explanation');if(headerButton){headerButton.textContent='EXPLANATION IN BATTLE STATUS';headerButton.classList.add('available')}if(r.defeat){setTimeout(()=>alert(msg),200)}return}setTimeout(()=>{if(r.defeat){if(confirm(msg+' Retry?'))api('/api/battle/retry',{session_id:session.session_id}).then(render)}else if(r.defeated){if(confirm(msg+' Continue to next node?'))api('/api/battle/next-turn',{session_id:session.session_id}).then(x=>{if(x.victory)alert('SPECTRAL CHAMPION — all chapters complete!');render(x)})}else alert(msg)},200)}
function showOutcome(r){const msg=r.defeated?`VICTORY — ${r.boss.name} defeated.`:r.defeat?'DEFEAT — retry to regroup.':r.correct?`DIRECT HIT — ${r.damage} damage. ${r.boss_hit?'Counterattack!':'Boss missed!'}`:`SPELL FIZZLE — correct answer: ${r.correct_answer}`;if(!r.correct&&!r.defeated){window.lastExplanation=r;ensureExplanationUi();const headerButton=$('#view-explanation');if(headerButton){headerButton.textContent='EXPLANATION';headerButton.classList.add('available')}showExplanation(r);if(r.defeat){setTimeout(()=>alert(msg),200)}return}setTimeout(()=>{if(r.defeat){if(confirm(msg+' Retry?'))api('/api/battle/retry',{session_id:session.session_id}).then(render)}else if(r.defeated){if(confirm(msg+' Continue to next node?'))api('/api/battle/next-turn',{session_id:session.session_id}).then(x=>{if(x.victory)alert('SPECTRAL CHAMPION — all chapters complete!');render(x)})}else alert(msg)},200)}
